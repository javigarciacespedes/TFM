input {
  file {
    path => "/ruta/a/tu/archivo.csv" # Cambia esto a la ruta real de tu archivo CSV
    start_position => "beginning"
    sincedb_path => "/dev/null"
  }
}

filter {
  csv {
    separator => ","
    columns => ["Operación", "Repetición", "Tiempo (ns)"]
    skip_header => true
  }
  mutate {
    convert => { "Repetición" => "integer" }
    convert => { "Tiempo (ns)" => "integer" }
  }
}

output {
  elasticsearch {
    hosts => ["http://localhost:9200"] # Cambia esta URL si Elasticsearch está en otro lugar
    index => "nombre-del-indice"
  }

  stdout {
    codec => rubydebug
  }
}
