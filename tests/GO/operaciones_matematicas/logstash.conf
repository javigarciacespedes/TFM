input {
  file {
    path => "/ruta/a/tu/archivo/testMathGo.log"
    start_position => "beginning"
    sincedb_path => "/dev/null"
  }
}

filter {
  grok {
    match => { "message" => "%{GREEDYDATA:operacion} - Repetición %{INT:repeticion}: %{INT:tiempo}ns" }
  }
}

output {
  elasticsearch {
    hosts => ["localhost:9200"]
    index => "benchmark-%{+YYYY.MM.dd}"
  }
  stdout {
    codec => rubydebug
  }
}
