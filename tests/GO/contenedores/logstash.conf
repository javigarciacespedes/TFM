input {
  file {
    path => "/ruta/a/tu/testContGo.log"
    start_position => "beginning"
    sincedb_path => "/dev/null"
  }
}

filter {
  grok {
    match => { "message" => "Iteración %{NUMBER:iteracion}: %{TIME:tiempo}" }
  }
  date {
    match => [ "tiempo", "HH:mm:ss.SSS" ]
    target => "tiempo"
  }
}

output {
  elasticsearch {
    hosts => ["localhost:9200"]
    index => "tiempos_creacion_contenedor"
  }
  stdout { codec => rubydebug }
}
