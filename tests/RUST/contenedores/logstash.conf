input {
  file {
    path => "/Users/javiergarciacespedes/Desktop/TFM/tests/RUST/contenedores/testContRust.log"
    start_position => "beginning"
    sincedb_path => "/dev/null"
  }
}

filter {
  grok {
    match => { "message" => "Iteración %{INT:iteration} - Tiempo de creación: %{INT:creation_time}ns" }
  }
}

output {
  stdout {
    codec => rubydebug
  }
}
